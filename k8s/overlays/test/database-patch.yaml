apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: database
spec:
  template:
    spec:
      nodeSelector:
        agentpool: dbpool
      tolerations:
        - key: "workload"
          operator: "Equal"
          value: "db"
          effect: "NoSchedule"